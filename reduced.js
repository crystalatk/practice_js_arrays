'use strict';

// Methods used!
console.log('***methods used***');
const arr = [1, 2, 3, 4, 5, 6];
const reduced = arr.reduce((total, current) => total + current);
console.log(reduced);


// my function
console.log('\n\n***')